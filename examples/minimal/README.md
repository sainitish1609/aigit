# Minimal aigit example

This is the smallest useful `aigit` intelligence repository shape.

## Try it

```bash
cd examples/minimal
aigit doctor
aigit lock
aigit snapshot -m "minimal baseline"
```

## Files

- `intelligence.yaml` — AI system manifest
- `prompts/system.md` — prompt file hashed by the lock command
